package main

//"time"

func isExpired(setDate, nowDate string, days int) bool {
	// passwordDate, _ := time.Parse(time.DateOnly, setDate)
	// fmt.Println(passwordDate)

	// currentDate, _ := time.Parse(time.DateOnly, nowDate)
	// expirationDate := passwordDate.Add(time.Hour * 24 * time.Duration(days))
	// fmt.Println(expirationDate)

	// return expirationDate.Before(currentDate) || expirationDate.Equal(currentDate)

}

func main() {

	// dataStr := "2000-01-01"
	// currentDateStr := "2001-01-01"
	// day := 10

	// res := isExpired(dataStr, currentDateStr, day)
	// fmt.Println(res)

}
